@page
@{
    ViewData["Title"] = "Student Dashboard";
}

<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="mb-1">Student Dashboard</h1>
            <p class="text-muted mb-0">Course enrollment view will come next</p>
        </div>
        <a href="/Index" class="btn btn-outline-secondary">Back</a>
    </div>

    <div class="card shadow-sm rounded-4">
        <div class="card-body">
            <p class="mb-0">This page is ready for the next feature: viewing and enrolling in course classes.</p>
        </div>
    </div>
</div>